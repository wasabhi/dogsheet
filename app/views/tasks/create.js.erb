$('#timeslice_task_id').replaceWith('<%= escape_javascript(
  collection_select(:timeslice, :task_id, Task.all, :id, :name_with_prefix)
) %>');
$('#timeslice_task_id').val(<%= @task.id %>);
$('#timeslice_finished_time').focus();
$('#task_name').val('');
