<h1>Tasks for <%= @client.name %></h1>

<ul>
  <% for task in @tasks %>
  <li><%= link_to task.name, task_path(task) %> (<%= task.duration %>)</li>
  <% end %>
</ul>

<%= link_to 'New task', new_client_task_path(@client) %>
<%= link_to 'Back to client', @client %>
