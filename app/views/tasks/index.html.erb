<h1>Tasks</h1>

<ul>
  <% for task in @tasks %>
  <li><%= render :partial => task %></li>
  <% end %>
</ul>

<ul id="task-tree">
<% @tasks.each do |t| %>
<%= task_tree(t) if t.root? %>
<% end %>
</ul>

<%= link_to 'New task', new_task_path %>
