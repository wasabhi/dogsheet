<h1>Tasks</h1>

<ul>
  <% for task in @tasks %>
  <li><%= link_to task.name_with_ancestors, task_path(task) %> (<%= task.duration %>)</li>
  <% end %>
</ul>

<ul id="task-tree">
<% Task.roots.each do |t| %>
<%= task_tree(t) %>
<% end %>
</ul>

<%= link_to 'New task', new_task_path %>
