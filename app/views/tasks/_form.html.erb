<%= form.error_messages %>
<div class="form-item">
  <%= form.label :parent_id %>
  <%= form.collection_select :parent_id, @tasks, :id, :name_with_ancestors, {:prompt => '-- Top level --'}, {:class => 'autocomplete'} %>
</div>
<div class="form-item">
  <%= form.label :name %>
  <%= form.text_field :name %>
</div>
<div class="form-item">
<%= form.submit('Add task') %>
</div>
